.SILENT:

default:
	echo "Please switch to a development branch. 
	echo "This branch is only for stable releases."
	echo
	echo "To see what branches you already have: 'git branch -l'"
	echo "To see all branches: 'git branch -a'"
